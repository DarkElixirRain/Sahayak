fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
python-dotenv>=1.0.1
psycopg[binary]>=3.2.0
psycopg-pool>=3.2.0
# passlib 1.7.4 is incompatible with bcrypt 5.x (which raises on >72-byte
# secrets instead of truncating, breaking passlib's backend self-test).
# Pin bcrypt<5 until passlib is replaced/none is possible.
passlib[bcrypt]>=1.7.4
bcrypt<5
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.9
mutagen>=1.47.0
google-cloud-speech>=2.28.0
google-cloud-texttospeech>=2.17.0
email-validator>=2.1.0

# Development / testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0